apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "instituto-next.fullname" . }}-test-connection"
  labels:
    {{- include "instituto-next.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "instituto-next.fullname" . }}:{{ .Values.app.service.port }}']
  restartPolicy: Never
